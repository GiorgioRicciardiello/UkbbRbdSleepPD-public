# pip requirements for UkbbRbdSleepPD (RBD -> Parkinson's disease analysis)
# Conda is the primary, fully-pinned environment (see environment.yml).
# This file is provided for pip-only / venv users.
#
#   python -m venv .venv && source .venv/bin/activate   # (Windows: .venv\Scripts\activate)
#   pip install -r requirements.txt
#
# Requires Python 3.11.

# ── Core scientific stack ──────────────────────────────────────────────
numpy>=1.26,<2.0          # <2.0: lifelines 0.30 uses deprecated np.bool_ aliases
pandas>=2.1,<3.0
scipy>=1.11
scikit-learn>=1.3
joblib>=1.3

# ── Data formats ───────────────────────────────────────────────────────
pyarrow>=14.0
openpyxl>=3.1

# ── Visualisation ──────────────────────────────────────────────────────
matplotlib>=3.8
seaborn>=0.13
plotly>=5.0

# ── Statistics / survival ──────────────────────────────────────────────
statsmodels>=0.14
lifelines==0.30.0         # pin exactly: API changed 0.29 -> 0.30
autograd==1.8.0
autograd-gamma==0.5.0
formulaic>=0.6
interface-meta>=1.3
wrapt>=1.16

# ── Utilities ──────────────────────────────────────────────────────────
tabulate>=0.9
pyyaml>=6.0
tblib>=2.0
rapidfuzz>=3.0
fuzzysearch>=0.8
beautifulsoup4>=4.12
html5lib>=1.1
soupsieve>=2.4
webencodings>=0.5
